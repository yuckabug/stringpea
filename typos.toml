[files]
extend-exclude = [
    "*",
    "!LICENSE",
    "!NOTICE.md",
    "!README.md",
    "!package.json",
    "!src",
    # Ignore the `confusables.json` file as it is generated by a script
    "src/confusables.json",
]
